import { Configuration } from "@/components/game/Configuration";

export default function GamePage() {
  // This page will eventually show the game dashboard or the configuration screen.
  // For now, it will always show the configuration.
  return <Configuration />;
}
